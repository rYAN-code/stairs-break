let parameter = {
    model: 'beforeSplit',

    ladderWidth: 4820,
    ladderHeight: 3300,
    ladderDepth: 2900,

    stairWidth: 4420,
    stairHeight: 2900,
    stairDepth: 1400,
    stairNumber: 17,

    beamWidth: 200,
    beamHeight: 400,
    beamDepth: 4820,
    
    distance: 100,
    thickness: 180,
}
const data = {
    "model": parameter.model,

    "ladderWidth": parameter.ladderWidth,
    "ladderHeight": parameter.ladderHeight,
    "ladderDepth": parameter.ladderDepth,

    "stairWidth": parameter.stairWidth,
    "stairHeight": parameter.stairHeight,
    "stairDepth": parameter.stairDepth,
    "stairNumber": parameter.stairNumber,
    
    "beamWidth": parameter.beamWidth,
    "beamHeight": parameter.beamHeight,
    "beamDepth": parameter.beamDepth,
    
    "distance": parameter.distance,
    "thickness": parameter.thickness,
};

export {parameter, data}